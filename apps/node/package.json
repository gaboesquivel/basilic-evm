{
  "name": "@repo/nodejs",
  "version": "1.0.0",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/node": "latest",
    "ts-node": "^10.9.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.0.0",
    "@repo/tsconfig": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
